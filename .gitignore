node_modules/
tmp/
reference/
*.tgz

# 资产导出压缩包：对使用者无用（要 GLB，或自己 npm run assets:build 生成），
# 且每个几 MB，不进版本库。
assets/3d/*.zip

# 插件在**会话工作区**里写的状态快照副本：客户端据此用相对路径找到宿主状态。
# 纯派生数据，可随时删除。开发时工作区就是本仓库，所以这里也要忽略。
.dsh-studio/

# 官网独立部署，不随插件仓库发布。
/website/
/scripts/check-website.cjs
